{
  "name": "todo-manager",
  "version": "1.0.0",
  "description": "A web todo app to manger your task",
  "scripts": {
    "dev": "parcel src/index.html",
    "cleanup": "rm -rf .parcel-cache dist",
    "build": "npm run cleanup && parcel build ./src/index.html --no-source-maps",
    "lint": "eslint src/"
  },
  "keywords": [
    "todo"
  ],
  "author": "iamziike",
  "license": "MIT",
  "dependencies": {
    "animejs": "^3.2.1",
    "parcel": "^2.2.1",
    "redux": "^4.1.2"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.2.1",
    "@parcel/transformer-sass": "^2.2.1",
    "eslint": "^8.8.0"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
